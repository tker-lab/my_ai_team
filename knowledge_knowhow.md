# 運用ノウハウ(部署横断で使えるナレッジ)

> [knowledge.md](knowledge.md) から切り出したファイル。ルール自体はCLAUDE.mdにあり、こちらには「なぜそうするのか」「元になった失敗」を記録する。
> セッション開始時には読み込まれないため、背景を知りたい時に開いて参照する。

## 作成担当とチェック担当の分離(2026-08-15・動画部での指摘から)
- **内容**:成果物を作った本人(と同じ思考の流れ)がそのままセルフチェックすると、同じ見落としを繰り返しやすい。「作る人」と「チェックする人」を別セッション・別枠に分けて、チェック担当には修正させず指摘のみさせるのが有効
- **初適用**:動画部の字幕チェック(2026-08-15)
- **現在の適用範囲**:動画部・顧問部・アプリ開発部・広報部の4部署。ライフサポート部は対象外
- **各部署のチェックリストの場所**:それぞれの部署ファイルの「チェック工程」セクション

## 長時間作業の進捗確認(2026-08-22・顧問部での失敗から)

**確認の間隔**:最初の1回は**5分後**(起動直後のエラーを早く捕まえるため)、以降は**15分おき**。5分間隔だと大半が空振りになり、30分だと異常の発見が遅すぎるため、15分が実用的な落としどころ。

**何を見るか**:「動いているか」ではなく「**前回より妥当な量が進んだか**」を見る。作業内容(データ取得・動画編集・コード作成など)によって「妥当な量」の中身は変わるので、その作業なりの進み方をしているかで判断する。

**前提条件**:進捗が見えない作りのまま走らせない。まず途中経過が確認できる状態にしてから開始する。

**この教訓の元になった失敗**:顧問部のデータ取得作業で、処理が4時間動き続けた。こまめに確認はしていたが、①ログが溜め込まれて出力されない作りだったため何度見ても「動いている」としか分からず、②プロセスは生きておりCPUも使っていたので正常に見えたが、実際は失敗とリトライを延々繰り返していただけだった。「生きているか」の確認では異常を検知できないと判明した。

## セッションの分け方(2026-09-03にCEOと整理)

**原則:1用件 = 1セッション。** 「ずっと1本」でも「毎回新規」でもない。

- 話題が変わった / 日が変わった → 新しく始める
- 同じ用件の途中で中断した → `/resume` で続ける(シャットダウンしても履歴は消えない)
- 迷ったら新規でよい

**なぜ新規で困らないか**:決まったことを `status.md`・部署ファイル・メモリに書き出す運用にしているため、会話履歴が無くても秘書が現状を把握できる。会話を捨てられる作りが前提。

**なぜ1本で続けすぎないか**:コンテキストウィンドウの上限を超えると古い部分が要約に圧縮され、細部が失われる。1回あたりのコストも重くなる。

**トークン節約の本命はセッション分割ではなく部署への委譲。** 部署は自分の作業場で重い処理をこなし、秘書には要約だけを返す(実例:2026-09-03のスクリプト作成で担当は79,000トークン使用、秘書側に返ったのは報告文のみ)。逆に新規セッションは毎回 CLAUDE.md・secretary.md・knowledge.md・status.md の読み直しコストがかかるため、短いセッションを乱立させても節約にならない。

**CEOに依頼した習慣**:用件が片付いたら「この件は終わり」と一言もらう。秘書はそれを合図に記録へ反映しコミットする。これを言わずにセッションを閉じると記録が残らない — 唯一の落とし穴。

**「話題は違うが同時にやりたい」場合**:セッションを増やすのではなく**部署に振る**。秘書は1人のまま、部下を並列に走らせる形にする(実例:2026-09-03、アプリ開発部にスクリプト作成を投げている10分間、秘書は並行してAGENTS.mdを作成)。

**例外**:Apple IDログイン・GUI操作・ライセンス同意など**CEO本人しかできない操作**が混ざる作業は、部署が都度聞き返してくるため往復が増えて逆に遅い。この場合は別ウィンドウで直接進めてよい。**ただし条件として「記録とコミットはどちらか一方のセッションだけが担当する」と決めておくこと**(両方が書くとファイル内容とGitが衝突する)。

**同じファイルを触る作業を並列にしてはいけない理由**:サブエージェントは互いの作業を知らないため、2人が同じファイルを書き換えると後から書いたほうが前の変更を丸ごと潰す。2026-09-03のスクリプト作成→チェックを順番に実施したのはこのため(同一ファイルが対象)。運用ルール本体は secretary.md の「部署の並列起動」に記載。

**別セッションで直接作業することの本当のコスト**:トークンではなく、`status.md` や部署メモリに記録が残らないこと。後から経緯を追えなくなる。

## sudo(管理者権限)が要る作業はAI側で実行できない(2026-09-03・アプリ開発部での実例から)

Claude Code/Codexの実行環境は**対話でのパスワード入力ができない**ため、`sudo` を伴うコマンドは秘書・部署ともに原理的に実行不可。`sudo -n true` の時点で失敗する。

**部署への指示に必ず含めること:** sudoで詰まったら回避策を探さず、**CEOがターミナルにそのまま貼るだけで済む形のコマンドを、順番通り・1つずつ何をするものかの説明付きで列挙して報告する。**「できませんでした」で終わらせない。

該当しやすい作業:開発環境の設定変更(xcode-select等)、システム領域へのインストール、ライセンス同意、`/Library` 配下への書き込み。

**進捗・サイズの計測に `du` を使わない。** `du` はディスク割り当て量を返すため、事前確保された領域や圧縮の影響で実データ量とずれる(Xcode.appは `du` で3.5GB、実際は8.8GB)。実データ量は `find ... -exec stat -f %z` の合計で測る。

## iOSシミュレータの権限許可ダイアログは XCUITest で自動突破できる(2026-09-04・アプリ開発部)

**結論:「GUI操作が要るので確認できません」で止めない。** シミュレータ相手なら許可ダイアログも含めて自動化できる。

**確実な方法:XCUITest(画面を自動操作するテスト)の中からダイアログのボタンを直接タップする。**
コツが2つある。
1. **アラート/シートという「入れ物」を経由せず、ボタンを直接狙う。** iOS 26の写真許可ダイアログは内部的にアラートかシートかの判定が食い違っており、`.alerts` 越しに調べるとエラーになる。`springboard.buttons["フルアクセスを許可"]` のようにボタンを直接指定する
2. **待ち時間の長い問い合わせを使わず、10ミリ秒ごとに存在確認する。** 待ちが長いとその間にiOS側が先に「許可しない」を選んでしまうことがある

これで得られる許可はTCC上も「正規の手順で得た許可」として記録され、実機で手動タップしたのと同じ扱いになる。

**うまくいかない方法(試して失敗した記録):**
- `xcrun simctl privacy <UDID> grant photos <bundleID>` — 起動中に実行しても再起動で auth_value が 0 に戻る
- TCC.db(`~/Library/Developer/CoreSimulator/Devices/<UDID>/data/Library/TCC/TCC.db`)の直接書き換え — 停止中に既存行を `auth_value=2` に更新すると一度は通ったが、**アプリを再インストールすると行ごと消え、作り直しても効かなくなった**。再現性がないので当てにしない

**注意:アプリをアンインストールすると許可が失われる。** 検証中の安易な入れ直しは避ける。

**進捗・サイズの計測に `du` を使わない。** `du` はディスク割り当て量を返すため、事前確保された領域や圧縮の影響で実データ量とずれる(Xcode.appは `du` で3.5GB、実際は8.8GB)。実データ量は `find ... -exec stat -f %z` の合計で測る。
