# 写真スライドショー・タイマーアプリ

> 2026-09-03にCEOから発案。**アプリ開発部の1本目(最優先)**に決定。離乳食管理アプリ・ライフプランシミュレーターは2本目以降に後ろ倒し。

## コンセプト
タイマーが動いている間、iPhoneの写真フォルダの写真・動画が結婚式のオープニング映像のように流れる。動画は音付きで再生。

## 前提・制約(CEO決定事項)
- **リリースを前提とする**(App Store公開を視野に入れる)
- **サーバーを持たない。全処理を各個人の端末内で完結させる** — 運用費・障害対応・プライバシーリスクを負わないため
- 解析結果もiCloudに置かず端末内DBのみ

## 機能ごとの実現可否(2026-09-03 秘書調査)

| やりたいこと | 可否 | 備考 |
|---|---|---|
| タイマー + スライドショー | ⭕️ | 標準的な実装 |
| ランダム再生 | ⭕️ | |
| 動画を音付きで再生 | ⭕️ | タイマーのアラーム音との競合設計が必要 |
| 色(緑等)で指定 | ⭕️ | 自前解析 |
| 日付・場所・種別で指定 | ⭕️ | メタ情報。解析不要 |
| **人物ごと** | ❌ | Appleが顔・人物データを他社アプリに非公開。私的APIも封鎖済み |
| **メモリーごと** | ❌ | 同じく非公開 |

- 人物の代替:Visionで端末内の顔検出 → 顔の「指紋」化 → 類似度でグルーピング → CEOが名前付け。**Appleは「同一人物判定」専用APIを提供しておらず、純正の人物アルバムより精度は落ちる**
- メモリーの代替:撮影日時・位置情報から「2025年の夏」「京都で撮った写真」等を自前で構成

出典:[People/Places非公開の公式回答](https://developer.apple.com/forums/thread/688345) / [顔タグ非公開の公式回答](https://forums.developer.apple.com/forums/thread/126711) / [同一人物判定について](https://developer.apple.com/forums/thread/751504)

## 写真・動画から取れる情報(条件指定の材料)

**解析不要(メタ情報。即時取得)**
撮影日時・編集日時 / 位置座標・高度 / 写真・動画・Live Photoの別 / 細別(パノラマ・HDR・スクショ・ポートレート・スロモ・タイムラプス・シネマティック・連写・長時間露光・RAW・GIF) / 解像度・縦横比 / 動画の長さ / お気に入り★ / 編集済みか / 取得元(カメラ・他アプリ・PC同期) / EXIF(機種・レンズ・F値・ISO・シャッター速度・フラッシュ・フレームレート・音声トラック有無)

**標準スマートアルバム(そのまま選択肢化可能)**
最近追加した項目 / お気に入り / ビデオ / セルフィー / スクリーンショット / パノラマ / スローモーション / タイムラプス / 連写 / Live Photos / ポートレート / シネマティック / 長時間露光 / RAW

**要解析(Vision・CoreImage。すべて端末内完結)**
平均色・優勢色・明るさ・彩度 / 顔の人数・大きさ・位置・表情 / 被写体カテゴリ(Apple提供の数百種) / OCR / 類似画像のグルーピング / **「よく撮れてる度」スコア(iOS 18以降。スクショやメモ写真の自動除外に有効)** / 体・手のポーズ / 動物検出

## 設計方針:事前解析を最小化する4原則(2026-09-03決定)

CEOの懸念「アプリ起動時に全写真を解析する必要があるのでは。タイマーを使いたい時に待たされたら使われない」への回答。

1. **選択肢は固定リストにする。マスタを事前に作らない。** 色・カテゴリの選択肢はユーザーの写真を見なくても用意できる。フリー入力は一切使わない。事前解析が必須なのは「その人固有の値でないと選択肢が作れないもの」=場所と人物の2つだけ
2. **判定は選ばれた時に、流しながら行う(遅延評価)。** スライドショーは1枚ずつしか表示しないので全件の事前判定は不要。候補をシャッフルして順に判定し、合格したものから表示。表示速度より判定のほうが速いため追いつかれない
3. **解析結果は端末内DBに保存。同じ写真は二度と解析しない。** localIdentifierをキーに色・カテゴリ・スコアを蓄積。使うほど速くなる
4. **増減は差分だけ追う。全走査しない。** [`PHPersistentChangeToken` / `fetchPersistentChanges`](https://developer.apple.com/documentation/photos/phpersistentchangetoken)(iOS 16+)で「前回終了以降の追加・削除・変更」だけを取得。起動時の仕事は数枚の追加・削除のみで一瞬

補助策:BGProcessingTask(充電中かつ未使用時にOSが裏でアプリを動かす仕組み)で未解析ぶんを夜間に消化

## 選択肢の作り方(2026-09-03決定)

| 条件 | 選択肢の生成方法 |
|---|---|
| 色・雰囲気 | 固定8〜10個。細かく刻んでも精度が上がらないため代表色のみ。「暖色/寒色/モノトーン/鮮やか/淡い」等の雰囲気軸も併設を検討 |
| カテゴリ(犬猫・食べ物等) | Apple提供の数百カテゴリから20〜30個を抜粋。**空振り対策として起動後に200枚程度をサンプル解析し、多い順に並べる・0件のものを隠す** |
| 場所 | **その人の写真から自動生成。** 座標を読む(一瞬)→ 近いもの同士をクラスタリング(30〜50箇所に収束)→ **かたまりの中心だけ**地名変換。フリー入力不要、撮っていない場所は出ない。**地図上のピン/濃淡から指で選ぶUIも併設を検討** |
| 日付・種別・お気に入り | 標準のメタ情報・スマートアルバムをそのまま |

## MVPの範囲(2026-09-04 CEO確定)

**入れるもの**
- タイマー本体 / スライドショー / 動画の音付き再生 / ランダム再生
- 絞り込み条件:**日時 / メディアの種類 / スクショ除くモード / 自由なアルバムを選べる / 雰囲気(暖色系等) / 場所 / カテゴリ**

**見送り(すべて後から追加可能。設計4原則を壊さない)**
- よく撮れてる度(v1.1) / 被写体の人数(人物と同時) / 人物(v2) / 体のポーズ(最後)

### 判断軸(この順で重視)
1. 待ち時間ゼロで動くか(解析不要のメタ情報を最優先)
2. これが無いと"使えないアプリ"になるか
3. 後から足しても設計が壊れないか
4. 精度リスクが低いか
5. 実装量に対して体験がどれだけ伸びるか

※場所・カテゴリは軸1〜2では後回し候補だったが、CEO判断でMVPに追加。

### カテゴリの精度方針(2026-09-04決定)
- **専用の検出機能があるのは実質「犬・猫・人」だけ。**食べ物・海・山・花・夜景等に専用機能はなく一般分類で扱う
- 犬・猫・人は専用機能を使う(高精度)。それ以外は一般分類を使い、**見逃すより拾いすぎる側に倒す**(スライドショーでは多少の混入より取りこぼしのほうが害が大きい)
- **サンプル解析はメニューの並べ替えにのみ使う。カテゴリを隠さない。**探す処理は常に全写真が対象(原則2)。0件時は正直に「見つかりませんでした」と出す

## 「よく撮れてる度」について(v1.1候補・調査済み)
[CalculateImageAestheticsScoresRequest](https://developer.apple.com/documentation/vision/calculateimageaestheticsscoresrequest)(iOS 18以降)。構図・色・ブレ・露出から点数を出すほか、**「実用目的の画像か」のフラグ**が付き、スクショ・書類・レシート・メモ写真を識別できる。MVPの「スクショ除くモード」の精度を大きく上げられるため、v1.1の最有力。
- iOS 18は2024年9月リリース(現行はiOS 26で2世代前)。最低対応バージョンをiOS 18にするのは概ね安全だが、**普及率の確認はリリース直前に行う**

## 環境構築(完了)
- 2026-09-03 22:12 — Xcode 26.6 (Build 17F113) 導入完了。9.45GB、回線混雑で約1時間半
- 2026-09-04 06:2x — CEOがsudo4コマンドを実行し、xcode-select切替・ライセンス同意・runFirstLaunch・**iOS 26.5シミュレータランタイム**の導入まで完了。回線が636Mbpsまで回復し数分で終了
- **sudoを伴う操作はAI側で実行不可**(→ [knowledge_knowhow.md](knowledge_knowhow.md))

## 開発〜リリースのステップ
1. 環境構築 ✅ 完了
2. MVP実装 ✅ 完了(2026-09-04。`apps/PhotoTimer/`。ビルド成功・詳細は[PROGRESS.log](apps/PhotoTimer/PROGRESS.log))
3. シミュレータで動作確認 ✅ クラッシュ修正・秒単位タイマー対応・表示秒数/動画設定の追加まで完了(2026-09-04、XCUITestで自動確認。詳細は[PROGRESS.log](apps/PhotoTimer/PROGRESS.log)、テストコードは`apps/PhotoTimer/PhotoTimerUITests/`)
   - 絞り込みなしでスタートしてもクラッシュしないことを確認済み(原因はPHFetchOptions.predicateの扱い。修正済み)
   - タイマーはiPhone標準「時計」アプリと同じ分・秒ホイール式に変更(秒単位で自由に設定可能、下限1秒)
   - 写真1枚あたりの表示秒数・動画の再生時間の扱い(最後まで再生/指定秒数で切り上げ)を設定画面(歯車アイコン)から変更可能に(2026-09-04、端末内保存)
   - **写真アクセス許可はXCUITestから自動突破できることを確認済み(2026-09-04)。以後の検証でCEOの手動タップは不要**(springboardのボタンをコンテナ非経由・10msポーリングで直接タップする方式。手順はapp-team部署メモリに保存。他部署のiPhoneアプリ検証にも応用可能)
   - **チェック工程の指摘(致命的2件・中程度7件・軽微6件中5件)に対応完了(2026-09-04)。** 特に「起動のたびに全写真を再走査」「該当0件で無限ループして固まる」の2件を修正、原則4(差分のみ処理)を実際に満たす作りに変更。詳細はPROGRESS.log参照
   - **2回目のチェック工程の指摘(致命的2件・中9件・軽微9件、計20件)にも対応完了(2026-09-04)。** 1回目の修正が生んだ副作用(✕で閉じても解析が裏で走り続ける、場所の絞り込みが復帰不能になる等)を解消。マナーモードでアラームが鳴らない可能性への対応(バイブレーション併用)も実施。XCUITestを13シナリオに拡張し全て成功。詳細はPROGRESS.log参照
4. 実機テスト(CEOのiPhoneを接続。ここで初めて自分の写真で試せる)
5. **Apple Developer Program登録(年約$99)** ← CEOの支払い判断が必要。4の後でよい
6. アイコン・アプリ名・デザイン確定 ← CEOの好み
7. App Store Connectへ登録(スクショ・説明文・価格・年齢制限・プライバシー申告)
8. 審査提出(通常1〜3日。リジェクト時は修正して再提出)
9. リリース

※7のプライバシー申告は写真アクセスがあるため必須。**端末内完結・外部送信なしの構成は説明上有利。**

## 実機で確認できたこと(2026-09-05)
- **サイレント(マナースイッチON)でアラームが鳴ることをCEOが実機で確認。**2ラウンド外していた問題が解決。原因は `AudioServicesPlaySystemSound` がアプリの音声設定を一切見ない仕様だったこと(→ AVAudioPlayerで自前生成した音に変更)
- **場所機能が実機で正常動作。**CEOが位置情報付きの写真を1枚撮ったところ、地名が正しく表示された。座標のクラスタリングと地名変換は実環境で機能している(※具体的な地名はGit管理外)
- **「近い順に流す」方式を本物の写真6,296枚で実測:**候補準備0.18秒 / 最初の1枚まで0.26秒。「起動時に全走査しない」「開いたら即タイマー」の2大原則を維持

## CEO決定(2026-09-05)
- **振り返り一覧を新設。**タイマー終了後、その回に流れた写真・動画を一覧表示する。**記録はタイマー画面に戻った時点で破棄**(端末内に履歴を残さない)
- **写真の削除は振り返り一覧からのみ。**スライドショー中のゴミ箱は廃止(誤タップ防止と「流れている間は一期一会」の体験維持のため)
- **振り返り一覧は将来の有料機能の候補。**FeatureFlagsで切り分け可能にしておく(現在は無料で利用可)
- 説明画面から「犬猫人のみ高精度判定」の記述を削除

## CEO決定(2026-09-04 夜)
- **初回リリースに入れる(実装指示済み):** 2〜3枚先読み / よく撮れてる度 / バックグラウンド動作(※バックグラウンド中は写真の検索・表示は不要。復帰時に正しく表示できればよい) / 写真の削除機能
- **初回リリースに入れない:** 体のポーズ / 人物(個人の識別) / 被写体の人数 — 「いずれ入れるかも」
- **課金機能は後から切り分ける**(実装は後回し)
- 収益化は**道3(広告なし・課金のみ)が本命**とCEO所感。最終判断はリリース設計時

## 未実装リスト(2026-09-04 時点で洗い直し)

### A. MVPから外した絞り込み(当初CEOが挙げた11個のうち4つ)
| 機能 | 外した理由 | 目安 |
|---|---|---|
| よく撮れてる度 | 「上位◯%」の基準決めに全写真の解析が要り、待ち時間ゼロの原則と衝突 | v1.1 |
| 被写体の人数 | 顔検出が必要。人物と土台を共有するので一緒に作るのが効率的 | 人物と同時 |
| 人物(個人の識別) | 最も重く、精度もApple純正に劣る | v2 |
| 体のポーズ | 用途が限定的で体験を大きく伸ばさない | 最後 |

※「自由なアルバム」「場所」「カテゴリ」はMVPに実装済み。

### B. 実装の途中で保留にしたもの
| 項目 | 状況 |
|---|---|
| バックグラウンド動作 | アプリを閉じるとタイマーが止まりアラームも鳴らない。CEO判断で見送り中 |
| **複数枚先読み(2〜3枚先)** | **1枚先読みは実装済み。**2〜3枚先は候補を捨てずに順番管理するキューが必要で複雑なため見送り |
| 色判定のブロック分割 | 現在は写真全体の平均色。部分的な色(芝生の上の犬など)が薄まって拾えない。数ブロックに分けて判定すれば精度が上がる |
| 場所データの保存構造の作り替え | 写真1枚につき1レコードを簡易ファイルに保存。数万枚だと重くなりうる。実機6,296枚では問題出ず |
| 写真の許可の粒度 | iOSに「読み取り専用」の区分が存在せず `.readWrite` しか選べない。写真削除機能を入れるならこの権限が正当化される |

### C. 未確定(リリースに必須)
- **アプリ名・アイコン・デザイン** — 全部仮のまま
- 最低対応iOSバージョン(MVPはiOS 17.0で実装。リリース直前に確定)
- 動画の音とアラーム音の優先関係(仮決定のまま)

### D. 2026-09-04にCEOから出た新規要望(未着手)
- **写真の削除機能** — 流れた写真をその場で消せるようにする。**iOSが必ず削除確認画面を出すため誤削除は原理的に起きない**(+「最近削除した項目」に30日残る)。CEOは「標準機能ではなく課金機能にするか」と検討中
- **バナー広告** — 画面上部。**「全処理が端末内で完結、外部送信なし」という本アプリの根幹と衝突する**(詳細は下記)
- **課金** — 課金すると広告が消え機能が増える形。月額/買い切り/機能別買い切りは未定。**Apple Developer Program($99/年)への登録が前提**

## 広告とプライバシーのトレードオフ(2026-09-04 CEOへ説明)
本アプリは「写真を一切外部に出さない」ことを設計の絶対制約としてきた。広告はこれと構造的に衝突する。判断はリリース設計時に行う。詳細は下記の通りCEOへ説明済み。

## 未決事項
- タイマー自体の仕様 → **秒単位のホイール式(分・秒2列、iPhone標準「時計」アプリ相当)に確定**(2026-09-04)。アラーム音はMVPで仮決定のまま。**バックグラウンド動作は今回(チェック工程の指摘対応時点)もCEO判断で見送りのまま**(2026-09-04再確認)。詳細は実装報告参照
- 動画再生時の音とアラーム音の優先関係 → MVPで仮決定(カウントダウン終了時に動画を即停止しシステム音を1回鳴らす)。要CEO確認
- アプリ名・アイコン・デザイン(CEO判断)
- 最低対応iOSバージョン(リリース直前に確定。MVPはiOS 17.0で実装)

## 【要修正】近い順方式の穴(2026-09-05 CEO実機報告)

CEO報告:「犬で絞ったのに全然犬じゃないものが出てくる。判定レベル流石に下げすぎか?」

**原因(秘書の見立て):** 候補を6枚ずつの「窓」に区切り、窓の中で最も点数の高い1枚を出す方式のため、**近い順が窓の中でしか効いておらず全体順になっていない。** 写真6,296枚に対し犬が数十枚なら、ほとんどの窓に犬が1枚も含まれず、それでも「窓内で一番マシな1枚」を出すため犬でない写真が出続ける。

**修正方針:** 窓の中に一定水準以上の該当があればそれを出す。無ければ次の窓へ進む。全ての窓を見て1件も無かった場合のみ、最も近いものを出す。
→ 「必ず何か出る」と「選んだものがちゃんと出る」を両立できる。「犬なのに羊」も該当を出し切った後に起きるため、CEOが歓迎する方向に効く。

**あわせて確認すること:** 犬・猫の専用検出(VNRecognizeAnimalsRequest)が実際に機能しているか。現状は上記の穴に隠れて検証できていない。CEOの写真から実際に何枚検出されるかを数えること。

**注意:** この穴は「必ず何か出るように」という秘書の指示から生まれたもの。閾値を完全に廃止したのが行き過ぎだった。

### 対応済み(2026-09-05)
- 6枚窓方式は廃止。窓拡大は「犬が無い窓から非犬を必ず出す」構造を解消せず、初回待ちも増やすため不採用。
- 合格優先のストリーミング探索へ変更。犬・猫は専用検出を最大18枚または0.8秒だけ優先し、見つからない時は一般分類が実際に対象ラベルを返した「説明可能な近似候補」だけを表示する。無関係な窓内最高点は表示しない。
- 2枚先読みと端末内キャッシュは維持。表示時間が長いほど次候補を探せる時間も増える旨を表示設定へ追記。
- 実機6045静止画を読み取り専用で全走査:185.78秒、取得失敗0、犬専用検出25、一般分類のみの犬候補3。`VNRecognizeAnimalsRequest` が実経路で動作することを確認。
- 別チェック指摘対応:候補なし区間でも1回の探索を18件/0.8秒で必ず終了し、UIへ制御を返して次回呼び出しから続行。単一解析にも残り時間の上限を適用。先読みTaskは世代番号で旧Taskの完了処理と新Taskを分離。
- 再チェック対応:探索予算と世代付き最大2件キューを本体・テスト共有部品へ分離。utility/解析失敗を含む18件上限、0.8秒上限、旧世代拒否、キュー上限を直接テストして成功。
- 主題(雰囲気・色・カテゴリ)は全体で単一選択へ変更。日時・場所・アルバム等は従来どおり併用可。旧複数選択設定は読み込み時に1件へ安全に移行。
- 解析キャッシュを主題別の強一致/説明可能近似ID索引として利用し、既知候補を先にshuffle bagで表示。写真本体は保存せず、同じ一巡内で重複させない。
- タイマー終了後にその回の表示項目をグリッド表示。通常タップは全画面閲覧、削除モードでは個別選択→選択数付き確認→OS確認の順。タイマー画面を閉じると履歴を破棄。
- 実機6045枚規模の解析JSONは959KB(写真本体なし)。1万枚でも概算約1.6MBのため現時点で容量上限・積極掃除は不要。写真削除差分と解析版不一致は既存処理で除去/無効化。

## 3回目のチェック工程・指摘対応(2026-09-05)

高2件・中5件・低10件、計17件の指摘に対応完了。詳細・理由は[PROGRESS.log](apps/PhotoTimer/PROGRESS.log)参照。
- 雰囲気軸(暖色/淡い等)の近似判定を撤廃し完全一致のみに厳格化。カテゴリ軸の緩さは維持(CEO判断)。
- 振り返り削除の回帰テストがクエリ不一致で一度も削除確認フローを通っていなかった不具合を修正。
  修正の過程で、iOS26の`confirmationDialog`がキャンセルボタンを描画しない実機不具合も発見・`.alert`へ変更して解決。
- バックグラウンド用ローカル通知と重複してアラームが鳴っていた不具合(アプリを開いた瞬間/復帰時)を修正。
- 振り返りプレビューの読み込み失敗時に無言で回り続ける表示・二重完了防御の不足を修正。
- 該当0件が数分間「読み込み中」のまま変化しない問題に20秒のタイムアウトを追加(進捗表示は追加せず)。
- 検証用シミュレータの写真ライブラリを誤って消してしまい、生成手順不明のため代替アセットで復旧した
  (教訓は[app-teamメモリ]に記録)。今後シミュレータのセットアップ手順は記録に残す。
