# 動画部:字幕制作の自動化ワークフロー(2026-09-15構築)

## これは何をする仕組みか(要約)

撮影した動画から、**字幕ファイル(SRT)を自動で作る一連の作業**をパソコンにやらせる仕組みです。今までは「動画を見ながら人力で字幕を書き起こす」または「AIに丸ごと任せて誤字だらけになる」のどちらかでしたが、この仕組みは間に3つの工程を挟むことで精度を上げています。

1. **文字起こし**:音声からセリフをテキストにする(AI・無料・パソコン内で完結、ネットに音声を送らない)
2. **時刻合わせ**:字幕の表示タイミングを、実際に話し始めた瞬間にぴったり合わせる(今まで「3秒ごとに機械的に切り替わって話し始めとズレる」問題があったのを解消)
3. **誤字直し+改行**:CEOが作った「用語集」と「その回の要点メモ」を見ながら医療用語・固有名詞の誤字を直し、単語や文節の途中で改行しないように字幕を区切る

最後に、字幕を動画に焼き込む(=動画に直接文字を埋め込む)ところまでできます。ただし**出てくる字幕はあくまで「完成候補」**です。そのまま公開するのではなく、①別セッションでのチェック、②奥様の最終チェック、を必ず挟んでください(youtube_team.md「チェック工程」参照)。

将来、この仕組みの「1. 文字起こし」の部分だけを有料の高精度サービスに差し替えることも想定して作ってあります(差し替え方法は後述)。

---

## 使うもの(導入済み)

| 役割 | 導入したもの | 備考 |
|---|---|---|
| 文字起こし | whisper.cpp(large-v3モデル) | `~/my_ai_team/tools/whisper.cpp/` に導入済み。無料・Mac内で完結 |
| 時刻合わせ | WhisperX(日本語アライメントモデル) | `~/video_venv` に導入。1文字ごとの発話時刻を推定 |
| 改行位置の判定 | BudouX(Google製、日本語の自然な分かち書きツール) | `~/video_venv` に導入 |
| フィラー「あの」の判定 | fugashi + UniDic(フル辞書) | `~/video_venv` に導入。「あの」が指示語(連体詞)かフィラー(感動詞)かを品詞で判定する(完全ではない。後述) |
| CEOが自分で字幕を直すアプリ | **Subtitle Edit**(Aegisubの代替。理由は下記) | `/Applications/Subtitle Edit.app` |
| 字幕の焼き込み | ffmpeg-full(字幕焼き込み機能:libass付き) | 通常の`ffmpeg`コマンドとは別に `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg` として導入(既存の`ffmpeg`はそのまま) |

### Aegisubではなく Subtitle Edit を導入した理由

指示ではAegisub導入予定でしたが、`brew install --cask aegisub` が **「macOSのGatekeeperチェックに通らないため2026-09-01付でHomebrewから無効化されている」** という理由で失敗しました(Aegisub開発元がApple公証を取得できていないため)。調査の結果、同種の無料字幕編集ソフト **Subtitle Edit** はApple公証済み(署名の正当性が確認できるアプリ)でHomebrewから問題なく導入できたため、これに切り替えました。波形表示・SRT編集など必要な機能はAegisubと同等です。

- もしどうしてもAegisubを使いたい場合は、公式サイト(aegisub.org)から直接ダウンロードし、初回起動時に「システム設定 > プライバシーとセキュリティ」でCEO自身が手動で許可する操作が必要です(この操作はCEO本人でないとできません)

---

## 中間フォーマット(将来、文字起こしを有料サービスに差し替える時のための設計)

このパイプラインは3段階のファイルを経由します。**後工程(誤字直し・改行・SRT化)は中間JSON(`*.aligned.json`)の形さえ合っていれば、文字起こしの中身がwhisper.cppでも他のサービスでも気にせず動きます。**

```
音声/動画ファイル
  → (transcribe_align.py) → *.aligned.json  ← 文字起こしを差し替える場合はここを作るスクリプトだけ入れ替える
  → (correct_and_wrap.py) → *.srt / *.corrected.json
```

`*.aligned.json` の中身(抜粋):
```json
{
  "segments": [
    {
      "id": 0, "start": 2.24, "end": 3.84,
      "text": "はじめまして、こたろうと申します。",
      "chars": [
        {"char": "は", "start": 2.24, "end": 2.29, "score": 0.65},
        ...
      ]
    }
  ]
}
```

将来ElevenLabs Scribe等に差し替える場合、そのサービスの出力をこの形(1文字ずつの開始・終了時刻付き)に変換する小さなスクリプトを1本足すだけで、`correct_and_wrap.py`以降はそのまま使えます。

---

## 使い方(1本の動画ごとの手順)

### 【重要】カット編集と字幕作成の順番

実際の運用は **撮影 → 不要な部分をカットして編集する → その「カット後の動画」に対して字幕を作る**、という順番にしてください。**このパイプラインは、必ずカット編集が終わった後の動画(または音声)に対して実行すること。**

理由:このパイプラインは音声から直接タイミングを取るため、**カット前の生の撮影データ(NGや言い直し、独り言を含む)にそのまま実行すると、その部分の字幕まで作られてしまいます**(2026-09-15の試運転で実際に確認:未編集のIMG_3163.MOVに実行したところ、まとめパートの撮り直し重複や「もうカットっすね」「どうしようかな」等の独り言まで字幕化された)。

**正しい手順**:
1. 撮影する(元動画は`元動画/`フォルダに置いたまま触らない)
2. 動画編集ソフト等で不要な部分(NG・言い直し・独り言)をカットし、`編集中/`フォルダに「カット済みだが字幕なし」の動画を作る
3. **そのカット済み動画から音声を書き出し、このパイプラインを実行する**(以下の手順1〜)

**すでにカット前提で作った字幕を、カット後の動画に付け替えたい場合**(例:先に字幕だけ作ってしまった、または一部の並び替えだけ後から発生した):
- 一番簡単なのは、**カット後の動画から音声を書き出して、このパイプラインを最初からやり直すこと**(文字起こしからやり直しても数分程度で終わるため、手動でタイムスタンプを付け替えるより早く確実)
- どうしても付け替えが必要な場合は、「カットで残した区間のリスト(開始・終了時刻)」を作り、各字幕の時刻がその区間内に収まっていれば「その区間より前の、残した区間の合計時間」を足し引きして新しい時刻に変換する、という計算が必要になります(手作業だと分秒の単位変換ミスが起きやすいため、原則はやり直しを推奨)

### 0. 事前準備(1回だけ、CEOがやること)
1. 撮影後、`~/my_ai_team/youtube_team_episode_notes_template.md` をコピーして `youtube_team_episode_notes_XX.md`(XXは動画番号)を作り、その回で話した内容の要点を数行書く
2. 出てきた専門用語・固有名詞で用語集に無いものがあれば `~/my_ai_team/youtube_team_glossary.md` に追記する(表に1行足すだけ)

### 1. 音声の書き出し(動画部がやる)
**カット編集が終わった動画**(MOV等)から音声だけをWAVに変換する。
```bash
ffmpeg -i "カット済み動画.MOV" -vn -acodec pcm_s16le -ar 16000 -ac 1 "音声.wav"
```

### 2. 文字起こし+時刻合わせ
```bash
~/video_venv/bin/python3 ~/my_ai_team/tools/subtitle_pipeline/transcribe_align.py \
  "音声.wav" "出力先/接頭辞"
```
→ `出力先/接頭辞.aligned.json` ができる(5分の音声で1〜2分程度)

### 3. 誤字直し+改行+SRT出力
```bash
~/video_venv/bin/python3 ~/my_ai_team/tools/subtitle_pipeline/correct_and_wrap.py \
  "出力先/接頭辞.aligned.json" "出力先/接頭辞_candidate"
```
→ `出力先/接頭辞_candidate.srt`(完成候補の字幕)ができる

### 4. 人(Claude)による仕上げ
ステップ3の自動処理は用語集にある誤字・フィラー・改行のみ対応します。それ以外(言い回しの自然な整理、意味の完結点の最終判断など)は、この`.srt`を youtube_team.md のガイドラインと照らしながら目視で仕上げてください。

### 5. 字幕の焼き込み(必要な場合)
```bash
/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg -i "動画.mp4" \
  -vf "subtitles=字幕.srt:force_style='FontSize=20,PrimaryColour=&H00FFFFFF,OutlineColour=&H00000000,BorderStyle=1,Outline=2,Shadow=0,Alignment=2,MarginV=40'" \
  -c:a copy "出力動画.mp4"
```
※ 通常の `ffmpeg`(PATHにあるもの)には字幕焼き込み機能(libass)が入っていないため、必ず `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg` のフルパスを使うこと。

### 6. CEOが字幕を自分で直したい場合
`Subtitle Edit.app` でSRTファイルを開き、波形を見ながら文字・タイミングを調整できます。

### 7. チェック工程へ
youtube_team.md「チェック工程」の通り、作成担当とは別セッションでチェックリストと照合してください。

---

## このパイプラインの既知の限界(チェック時に特に注意)

- **whisper.cppが音声によって句読点を一切打たないことがある**:2026-09-16、IMG_3163.MOVで全編句読点0個という不具合が発生した(IMG_3156.MOVでは発生せず)。原因は特定できていないが、句読点入りの日本語文を初期プロンプト(`--prompt`)として与え、`--carry-initial-prompt`で全編に効かせ続けることで再現しなくなることを確認し、`transcribe_align.py`にデフォルトで組み込んだ。**文字起こし直後(whisper.cppの生JSON)の時点で読点・句点がゼロに近い場合は、この不具合が再発している可能性が高いので、まず文字起こしをやり直すこと**
- **用語集に無い誤字は直らない**:用語集は「よくある誤認識パターンの完全一致置換」でしか動かないため、初めて出てくる誤字は用語集に追記されるまで直りません
- **改行位置はBudouX(自動の分かち書き)ベース**:文節の塊を壊さないよう補正処理(「という」「ていただく」「なる/なり/なっ等の活用形」「べき」等の続き言葉や、指示語「この/その」が行末に孤立しないよう結合する処理)を入れており、さらに**fugashi(形態素解析)で、BudouXの塊の境界が単語の内部を割っていないかを機械的に検査し、割っていれば強制的に結合する安全装置**(`enforce_word_boundaries`)も追加した(2026-09-16、「置いと/いて」のような単語内部の分断を検出・修正できることを確認)。ただし「2つの塊がそれぞれ単語としては正しいが、まとめて1つの意味のまとまりとして改行しない方が自然」というケース(例:「〜という」等の機能語)は個別にパターンを追加する方式のままで、完璧ではありません。特に長い複合的な言い回しは不自然な位置で区切られる可能性が残るため、**「1行目→2行目の折り返しが文節の途中になっていないか」は必ず目視確認**してください
- **WhisperXのアライメントが稀に失敗する**:1セグメントだけ音声とうまく合わせられないことがあります(実績:10分の音声中1箇所)。その場合はセグメントの区間内で文字を均等割りした仮の時刻を使うため、その箇所だけ字幕タイミングの精度が落ちます(±1秒程度に戻る)。ログ(標準エラー出力)に `WARNING: segment N had no char alignment` と出るので、該当箇所は特に注意して確認してください
- **句点(。!?)で区切っていますが、読点(、)や「意味の完結点」の判断はしていません**:1つのVAD区間(無音で区切られた範囲)が長い場合、文字数の都合で機械的に複数の字幕に分割されることがあります。「呼吸の切れ目ではなく意味の切れ目で区切る」という最終判断は人(Claude)が行ってください
- **フィラー「あの」の判定は完璧ではない**:「あの」には指示語(例:「あの人」「あの経験」)とフィラー(単なる言い淀み)の2つの用法があり、品詞判定(UniDicフル辞書)だけでは区別しきれません。実装では「UniDicが連体詞と判定した場合は必ず残す」「フィラーと判定された場合、直後が具体的な名詞ならおそらく指示語として残す(ただし『皆さん』『次回』等の一般的すぎる語のリストに一致する場合は除く)」というヒューリスティックを使っていますが、2026-09-15夜の検証では同じ動画内でも「あの経験にも感謝」は正しく残せた一方、「あの一本目の動画のまとめに」のように判定が割れるケースが残っています。**「あの」が2箇所以上残っている場合は、削除して不自然にならないか一つずつ目視確認してください**

---

## 試運転の結果(2026-09-15〜16、詳細は部署メモリ参照)

IMG_3156.MOV(302秒)・IMG_3163.MOV(609秒)で試運転し、既存の比較用SRT(`scratch_3156.srt`等、簡易文字起こしで作成した旧データ)と比較した結果、およびチェック担当からの指摘を受けた改善(フィラー「あの」の判定・改行の一般化ルール追加・断片先頭の解消・誤字修正)の結果は、秘書への完了報告および `.claude/agent-memory/youtube-team/` のメモリファイルを参照してください。

---

## 今後のアイデア(未着手・実現方法の案のみ)

### カット候補の自動リストアップ

「カット編集と字幕作成の順番」で触れた通り、現状はカット編集を先に人手で終わらせる前提です。将来、**文字起こし結果から「撮り直しの重複」「独り言」をAIが見つけてカット候補の一覧を作る**ことができれば、カット作業自体も楽になる可能性があります。実現方法の案:

1. **撮り直し(重複)の検出**:`transcribe_align.py`が吐き出すセグメント(文単位)を順番に見ていき、近く(例:前後10セグメント以内)にある2つのセグメントのテキストが似ていないかを比較する(Pythonの`difflib.SequenceMatcher`で文字列の類似度を計算し、例えば0.6〜0.7以上なら「同じ内容を言い直している」と判定する)。似ている場合、**後に話された方が「本番」で前が「NG」であることが多い**という前提で、前のセグメントをカット候補としてリストアップする(逆のケースもあるため、あくまで候補であり最終判断は人が行う)
2. **独り言・メタ発言の検出**:「カットっすね」「どうしようかな」「もう一回」「NGです」「今の無し」「えーっと何だっけ」のような、動画の内容ではなく収録中の地の発言によく出てくる言い回しのリストを用意し、セグメントのテキストがそれに一致・部分一致したらカット候補としてリストアップする(用語集と同じような「一覧を作って随時追記する」形にすれば、新しい口癖が見つかるたびに強化できる)
3. **無音区間の一覧化**:VAD(無音検出)は`transcribe_align.py`ですでに使っているため、長い無音区間(例:3秒以上)の一覧も一緒に出せば、カットの切れ目の候補としてそのまま使える
4. **出力イメージ**:「開始時刻・終了時刻・該当テキスト・判定理由(重複/独り言/長い無音)」を一覧にしたCSVやMarkdown表を作り、CEOまたは動画部がそれを見ながら実際にカットするかどうかを判断する、という使い方を想定(自動でカットまではしない)

上記はいずれも実装しておらず、今夜は着手していません。次に着手するとよいタイミングは、カット作業自体の負担が字幕作業以上に大きくなってきたときだと考えられます。
