# AIチーム運営設計 — 引き継ぎ資料

> このファイルは、2026-08-22に秘書セッションで行った「運営ルールの置き場所設計」の議論をまとめた引き継ぎ資料です。
> **次のセッションでは、この内容を前提に「図解付きの資料作成」と「全ルールの棚卸し」を行う予定です。**

---

## 1. なぜこの議論が起きたのか(経緯)

顧問部の海外畜産統計プロジェクトを進める中で、**秘書が全ての実装を自分で行ってしまい、顧問部のサブエージェントを一度も立ち上げないまま**セッションが進行した。CEOの指摘で発覚。

「秘書は実装せず部署に振る」というルールは**メモリには存在していた**が、守られなかった。原因を調べたところ、構造的な問題が判明した。

### なぜルールが守られなかったか
- そのルールは**メモリ**に置かれていた
- メモリはセッション開始時に**目次(MEMORY.md)の一行しか読み込まれない**
- 長い会話の中で「リサーチ(秘書の仕事)→設計相談→実装」と地続きに移行したため、境界で立ち止まれず、引き出しを開けに行かなかった

→ **重要なルールほど、確実に読まれる場所に置く必要がある**という学びに至った。

---

## 2. 情報の置き場所と、その性質(調査で判明した事実)

### セッション開始時に自動で読み込まれるもの

| ファイル | 中身 | 読み込まれ方 |
|---|---|---|
| `CLAUDE.md` | プロジェクトのルール本文 | **全文** |
| `MEMORY.md` | メモリの目次(一行見出しの一覧) | 全文だが、**あくまで目次のみ** |

個別のメモ(引き出しの中身)は、AIが「関連しそうだ」と判断した時だけ開く。

### 確実性の順序
1. **CLAUDE.mdに本文を書く** → 毎回必ず目に入る(最も確実)
2. **MEMORY.mdの目次に載せる** → 見出しは目に入るが、中身は開かないと分からない
3. **部署ファイルに書く** → その部署の話だと判断して開かないと読まれない

### CLAUDE.mdの階層(3階層ある)
1. **個人用**(`~/.claude/CLAUDE.md`)— 全プロジェクト共通。現在未使用
2. **プロジェクト用**(`my_ai_team/CLAUDE.md`)— 現在使用中。AIチーム全体に効く
3. **フォルダ別** — サブフォルダごとに置ける。ただし現在は全ファイルが1フォルダにフラットなため実質使えない

### 重要な注意:CLAUDE.mdは短く保つべき
「全部CLAUDE.mdに入れれば安全」ではない。書いた内容は毎回必ず読み込まれる=常に頭の中を占有するため、**長くなるほど一つ一つの指示が薄まり、結局守られにくくなる**。

### 補足:knowledge.md/secretary.md/status.mdは「メモリ」ではない(2026-08-22・CEOからの質問で判明)

CEOから「knowledge.mdってメモリですか?」という質問が出て整理した。

- **knowledge.md・secretary.md・status.mdは「メモリ」システムとは別物。** これらは`my_ai_team`フォルダの普通のプロジェクトファイルで、上記の「メモリ(引き出し)」「部署ファイル」のどちらでもない**第4のカテゴリ**にあたる。
- **なぜ確実に読まれるか:** CLAUDE.md自身(=毎回全文読まれる)が、「セッション開始時にまずこの3ファイルを読め」と明記しているため。CLAUDE.mdが確実に読まれる → その指示も確実に目に入る → 結果として3ファイルも確実に開かれる、という**間接的な確実性**を持つ。今回のセッション冒頭でも実際にこの手順通り読み込んでいる。
- **ただし、この確実性は「秘書セッション」限定。** CLAUDE.mdの「セッション開始時の必須動作」は「専属秘書として振る舞え」という秘書ペルソナ向けの指示であり、部署のサブエージェント(顧問部・動画部など)には意味をなさない。サブエージェントはCLAUDE.md本文自体は引き継ぐが、「秘書として knowledge.md を読め」という指示を自分ごととして実行する理由がない。
- **結論:** knowledge.mdは秘書にとっては「ほぼ確実に開かれる引き出し」だが、部署(サブエージェント)にとっては通常の部署ファイルと同様「開かれない可能性がある引き出し」のまま。部署に知っておいてほしいknowledge.mdの内容があれば、秘書が指示文で明示的に渡す(C)か、その部署の定義ファイルに書き写す(B)必要がある。

---

## 3. サブエージェントが親から引き継ぐもの(公式ドキュメントで確認済み)

| 引き継がれる | 引き継がれない |
|---|---|
| **CLAUDE.md** | **メモリ(MEMORY.md含む)** |
| gitの状態 | **親の会話履歴** |
| 親が渡した指示文 | |
| skills(指定時) | |

※例外:Anthropic提供の組み込みエージェント(Explore、Plan)はCLAUDE.mdを読み込まない
※例外:Fork型サブエージェントは親の全て(会話履歴・メモリ含む)を引き継ぐ

### この線引きが意味すること
- **Aカテゴリ(全部署共通ルール)はCLAUDE.mdに1回書けば全部署に効く** — 部署ごとに重複して書く必要はない
- **メモリは自動では渡らない** → 「原則は知らない。必要なものだけ秘書が渡す」という運用は、**既定の動作そのもの**
- **会話履歴も渡らない** → チェック担当は自動的に「経緯を知らない目」になる(チェック分離の目的に合致)

### 部署専用メモリが持てる
サブエージェントの定義ファイルに `memory: project` と書くと、**その部署専用の学習ノート**を持てる。
- 保存先は部署ごとに独立(`.claude/agent-memory/<部署名>/`)
- **親にも他部署にも見えない**
- 次回その部署が立ち上がった時に、自分のメモだけ読み込める

→ 「部署ごとに知見が蓄積されるが、横断はしない」という、機密分離の要件に合致する。

---

## 4. ルールの置き場所の分類(今回確定した設計)

**見分ける基準は「次の依頼でも同じことを言うか?」**

| 種類 | 判定 | 置き場所 | 具体例 |
|---|---|---|---|
| **A. 全部署・常時** | 次も言う<br>+ どの部署でも言う | **CLAUDE.md** | 秘書は実装しない<br>専門用語には解説を添える<br>待ちが出るならETAを伝える |
| **B. 部署固有・常時** | 次も言う<br>+ その部署だけ | **部署の定義ファイル**<br>(`.claude/agents/○○.md`) | 動画部:字幕は2行以内<br>顧問部:近似訳には必ず注記 |
| **B'. 部署の学習蓄積** | 部署が自分で学ぶ | **部署専用メモリ**<br>(`memory: project`) | (自動で貯まる) |
| **C. 今回だけ** | 次は違うかも | **秘書が指示文で渡す** | 今回は役員向けだから堅めに<br>今回は急ぎなので速度優先 |
| — | CEO個人の情報・好み | **秘書のメモリ** | 自宅住所など<br>※部署には自動で渡らない |

**Cの見分け方**:「今回は」「この件は」が自然に付くならC。
例:「今回は明朝体で」ではなく「常に明朝体」ならA。

### 補足:サブエージェントの定義ファイルについて
サブエージェントに「CLAUDE.md」は別途存在しない。**定義ファイルそのもの**(`.claude/agents/顧問.md` 等)が、その部署にとってのCLAUDE.md相当になる。

---

## 5. 「フォルダ別CLAUDE.md」と「サブエージェント」の違い

混同しやすいが、全く別の仕組み。

| | フォルダ別CLAUDE.md | サブエージェント |
|---|---|---|
| 何が起きるか | **同じAI**が追加の指示を読む | **別の担当者**が立ち上がる |
| きっかけ | そのフォルダのファイルを触った時(自動) | 秘書が呼び出した時 |
| 頭の中 | 秘書と共有(会話の流れも引き継ぐ) | 独立(会話の経緯を知らない) |

たとえ:
- フォルダ別 = 「**その部屋に入ったら壁の注意書きを読む**」(読むのは同じ人)
- サブエージェント = 「**別の担当者を呼ぶ**」(独立した頭で作業)

→ **「機密案件だから分離したい」「チェックは経緯を知らない目でやりたい」という目的にはサブエージェント一択。** フォルダ別では秘書が秘書のまま作業してしまうので分離にならない。

---

## 6. モデル(AIの種類)の使い分け

### 料金比較(100万トークンあたり)

| モデル | 入力 | 出力 |
|---|---|---|
| Haiku 4.5 | $1 | $5 |
| Sonnet 5 | $2 | $10 |
| Opus 5 | $5 | $25 |
| **Fable 5** | **$10** | **$50** |

※CEOが見た「Fable 5はクレジットが必要」という表示について、公式ドキュメント上に該当記載は見つからなかった。**料金が高いことへの警告と推測される**(Sonnet 5の5倍)。

### 設定方法
- **秘書(メインセッション)のモデル変更**:`/model opus` と入力する
- **サブエージェントのモデル**:定義ファイルに `model: sonnet` のように書いて固定する

### モデルの使い分け(2026-08-22・CEO確定)
- 秘書 → Opus 5(会話の質を重視)
- 作成担当 → Sonnet 5
- チェック担当 → **Sonnet 5**(Fableではなく)

理由:チェックに必要なのは「経緯を知らない別の目で見ること」であって、モデルの賢さそのものではない。同じSonnet 5でも別セッション・チェックリストのみを渡す形にすれば十分機能するはず。見落としが目立つようならOpus/Fableへ上げる、という段階的判断とする。

参考として、Fable 5は「常時思考モードでオフにできない」「セキュリティ関連の話題には特に厳しく拒否されることがある」という特徴があり、料金もSonnetの5倍と最も高い。普段使いには過剰スペックと判断し、今回は採用を見送った。

---

## 7. すでに実施済みの変更(2026-08-22)

### CLAUDE.mdに追記した内容
- **【最重要】秘書は実装しない — 必ず部署に委譲する**
  - 破られやすい状況(リサーチから地続きで実装に移行する時、会話が長時間続いた時、小さな修正だと思える時)を具体的に明記
  - 理由として、顧問部・ライフサポート部の機密分離設計に触れた
- **【重要】作成担当とチェック担当は分離する**
- ETAを伝えるルール

**2026-08-22 内容確認 → CEO確定。** その際、「詳細は youtube_team.md 参照」という参照先がおかしい(チェック担当導入は他部署にも予定しているのに、動画部の1ファイルだけを指しているのは不整合)との指摘があり、修正済み:
- 「このルールは全部署共通」と明記した上で、考え方の詳細は`knowledge.md`の「運用ノウハウ」(部署横断ナレッジ)を参照するよう変更
- 各部署固有のチェック観点・チェックリストは各部署ファイルに書く、という位置づけに整理(動画部の字幕チェックリストは引き続き`youtube_team.md`)

### メモリの更新
- `secretary-delegates-not-implements.md` に今回の再発経緯を教訓として追記し、「正典はCLAUDE.md側」と明記

---

## 8. 次セッションでやること

- ~~図解付きの資料作成~~ → ✅完了([運営台帳](operations_ledger.html) Artifactとして発行済み)
- ~~モデルの使い分けの決定~~ → ✅完了(2026-08-22確定。7章参照)
- ~~CLAUDE.mdの内容確認~~ → ✅完了(2026-08-22確定。参照先の不整合を修正済み。詳細は7章参照)
- ~~全ルールの棚卸し~~ → ✅完了([ルール台帳](rule_ledger.html) Artifactとして発行済み。詳細は11章参照)
- **チェック担当の導入**:保留(CEOから「導入はまだ待って」)
   - 導入対象:顧問部・アプリ開発部・広報部(**ライフサポート部は不要**とCEO明言)
   - 顧問部でのチェック観点(今回の案件を踏まえて):
     - 軸1:完成物(データ)が想定通りか — 期間・件数・欠損・和訳の空欄
     - 軸2:会社PCで「CEO以外の人」が運用できるか — コードの効率、手順の分かりやすさ、失敗時のリカバリ可能性

---

## 9. 新規構想:運営状態を確認できるアプリ/サイト(2026-08-22・CEOより)

図解付き資料([運営台帳](operations_ledger.html) Artifact)を作成する過程で、CEOから新しい構想が出た。

**背景**:今後もルール・部署定義・メモリは増え続ける。それを「いつでも確認できる状態」にしておく必要があるのでは、という問題意識。

**要件(現時点でCEOが明言した条件)**:
- ルール・設計・役割・定義・メモリの「今の状態」が見やすいアプリ or サイト
- リアルタイム更新は必須ではない。**手動で「更新」を押したら反映されるレベルで十分**
- **PC・スマホ両方で見られることが最低条件**

**現状の候補**:今回作成した「運営台帳」ArtifactがすでにURL経由でPC/スマホ両対応。中身を直して同じURLに再発行(redeploy)すれば「更新ボタンを押したら更新される」の要件を満たせる見込み。ただし現状の中身は「ルールの置き場所設計」の解説にとどまり、部署定義・メモリ一覧・knowledge.mdの用語などは含んでいない。フルの「運営状態ダッシュボード」にするには範囲の再設計が必要。

**方針(CEO明言)**:「アプリかサイトかその他か」の実装は、**仕様が確定してから**でよい。今すぐ作り込む必要はない。

**次にやること**:
- ダッシュボードに含める範囲を確定する(運営設計だけか、部署定義・メモリ一覧・knowledge.mdまで広げるか)
- 上記が固まったら、既存の運営台帳を拡張するか、部署(アプリ開発部など)に実装を委譲するかを判断する

---

## 11. 全ルールの棚卸しと、サブエージェント運用の実装(2026-08-22)

図解付き資料に続けて、CLAUDE.md・メモリ・部署ファイルに散らばる全ルールをA/B/B'/Cに仕分け直した。結果は [ルール台帳](rule_ledger.html) Artifactにまとめた(全ルール一覧・分類・棚卸しで見つかった発見・推奨アクションを掲載)。

### 棚卸しで見つかった一番の発見:部署が「本物のサブエージェント」になっていなかった

`.claude/agents/` フォルダ自体が存在せず、部署固有ルール(Bカテゴリ)の置き場所として設計していた「部署の定義ファイル」が実在しなかった。秘書が部署ファイル(youtube_team.md等)の中身をその都度会話に貼り付けて、汎用のサブエージェントに渡している状態だった。

**CEOより「サブエージェント運用は確定」との明言を受け、その場で実装した:**

- `.claude/agents/` に5部署分の定義ファイルを新設(youtube-team.md / app-team.md / pr-team.md / life-team.md / advisory-team.md)
- 各ファイルに `model: sonnet` を固定(2026-08-22 CEO確定の「作成担当=Sonnet」を反映)
- 各ファイルは対応する部署ファイル(youtube_team.md等)を参照する形にし、内容の二重管理を避けた

### 秘書自身のモデルも仕組み化(CEOからの質問がきっかけ)

CEOから「秘書にもモデルOpusを書き込める? 基本は必ずOpusで動くようにして、こっちがSonnet/Fableに手動で切り替えた時だけ変えられるようにしたい」と依頼があった。

`update-config` スキルで調査した結果、`.claude/settings.json` の `model` フィールドで「Claude Codeのデフォルトモデルを上書きする」設定が可能と判明。**`/Users/takahashitakayuki/my_ai_team/.claude/settings.json` に `"model": "opus"` を設定した。**

- これにより、秘書セッションは何も指定しなければ常にOpusで起動する
- CEOが画面上(またはコマンド)で手動でSonnet/Fableに切り替えた場合は、その回はそちらが優先される(settings.jsonの値は「起動時のデフォルト」であり、手動切り替えを上書きするものではない)

### 重複の解消と、格上げルールの新設(2026-08-22・CEO指示)

棚卸しで見つかった重複への対応として、CEOより「重複したらルールとして格上げ、という仕組みがあるといい」との提案があり、以下を実施した。

**CLAUDE.mdに追加した3ルール(すべて1行):**
- ルールが2箇所以上に重複していたら統合する
- 部署ファイルは概要+リンクのみに保つ(メモリからの格上げ)
- リサーチは秘書が一手に担い knowledge.md に集約する。各部署は自分でリサーチせず参照する(youtube_team.mdの重複を削除して統合)

**CEOからの重要な指摘 →「格上げしすぎるとCLAUDE.mdが膨らむ」問題への対応:**

当初は「重複を見つけたら即CLAUDE.mdに格上げ」というルールにしていたが、CEOより「これだと CLAUDE.md が多くなりそう」との指摘。運営台帳2章の「CLAUDE.mdは長くなるほど一つ一つの指示が薄まる」と矛盾するため、2点修正した:

1. **判断基準を「重複しているか」から「部署(サブエージェント)も読む必要があるか」に変更。** CLAUDE.mdが特別なのは「サブエージェントに引き継がれる唯一の場所」だから。部署も読む必要があるならCLAUDE.md、秘書だけでよければsecretary.mdに一本化する。これで重複しても全部がCLAUDE.md行きにはならない。
2. **CLAUDE.mdへの追記は1行に収める。** 理由や詳細は別ファイルに置きリンクする。見出し付きの節にしてよいのは「実際に破られた実績があり、理由まで毎回目に入れる必要がある」ものだけ(現状は「秘書は実装しない」「作成・チェック分離」の2つ)。この方針自体もCLAUDE.mdの該当箇所に注記として明記した。

結果、ルールを3つ追加してもCLAUDE.mdの増加は実質4行に収まった。

### B'(部署専用メモリ)の導入(2026-08-22・CEO決定)

**全5部署に `memory: project` を設定して導入した。**

**議論の経緯:** CEOより「貯めてもいい気がする。違和感があれば会話から気づけると思う」との意見。CEOのアウトプットへの指摘力は実証済み(この一連のセッションだけでも字幕の問題・モデル表示の食い違い・参照先の不整合を自力で発見している)。ただし秘書から1点、**部署メモリは会話に出てこないため、気づけるのは「出来が悪い」という症状までで、原因(メモの中身)は自動では目に入らない**という懸念を提示した。

**【重要な訂正・2026-08-22】** 上記の説明で秘書は当初「中身は秘書からも見えない」と伝えたが、**これは不正確だった**。CEOからの「ファイル見に行ったり、部署に聞けば教えてくれるのでは?」という指摘で判明:

- 部署メモリは**ローカルのファイル**(`.claude/agent-memory/<部署名>/`)なので、**秘書が直接開いて読める**
- 部署本人に「メモの中身を教えて」と聞くこともできる
- 正しくは「秘書のコンテキストに**自動では入らない**(放っておくと目に入らない)」であって、アクセス不能という意味ではない
- ただし**他部署には本当に渡らない**。ここは機密分離の狙い通りの本物の壁

したがってリスクは当初の説明より小さい。下記の報告ルールは「自動で目に入るようにする」ための仕組みであり、点検手段はほかにも2つある(秘書がファイルを直接読む/部署に聞く)。

**対策としてセットで導入したルール(CLAUDE.mdに1行):**
> 部署が自分のメモリに書き足したことは、完了報告に1行添えて秘書に伝える。秘書はそれをCEOへの報告に含める。

これにより、貯まっていく内容がCEOの目に触れ、症状ではなく原因の段階で気づける状態を保つ。

**なお、この報告ルールをCLAUDE.mdに置いたのは、直前に新設した格上げルールの初適用でもある**(5つの部署定義ファイルに同じことを書けば重複になり、かつ部署も読む必要があるルールのため)。

### チェック担当の導入(2026-08-22・CEO指示により実施)

保留されていたチェック担当を、顧問部・アプリ開発部・広報部の3部署に導入した(ライフサポート部は不要とCEO明言、動画部は2026-08-15に導入済み)。

各部署ファイルに「チェック工程:チェック担当に渡すチェックリスト」セクションを追加:

- **顧問部**(advisory.md):当初決めていた2軸をベースに、実際にこのプロジェクトで起きた失敗を項目化。①データが想定どおりか(取得開始年・件数・API上限での無言欠落・和訳の空欄・近似訳の注記)②CEO以外が運用できるか(1行ずつ処理になっていないか・パスの直書き・手順書だけで実行できるか・失敗時の再開・送付ファイルが最新版か)
- **アプリ開発部**(app_team.md):機能の過不足/勝手な追加/落ちないか/エラー表示/家族の情報の外部送信/初心者が読んで分かるか
- **広報部**(PR_team.md):プライバシー(実名・顔・特定情報)/事実確認/チャンネルのトーン/誘導先/各チャネルの制約/本業情報の混入

**あわせて重複を1件解消:** 動画部の定義ファイル(`.claude/agents/youtube-team.md`)に「チェック担当として呼ばれたら修正せず指摘のみ」という説明が書かれていたが、これはCLAUDE.mdの記載と重複していた。5部署に同じことを書けばさらに重複するため、格上げルールに従い定義ファイル側を削除した(正典はCLAUDE.md)。

### knowledge.mdの切り出しとMD台帳の新設(2026-08-22)

**きっかけ:** CEOがYouTube動画(「Claude Codeで一番大事なのはCLAUDE.md」)で見た「MD台帳」という資料について、リアルタイム連携が可能か質問があった。スクリーンショットを確認したところ、**それもArtifactであり「実測 2026-08-16 20:52」という日時が焼き込まれたスナップショット**だと判明(フォルダのタブ切り替えは、あらかじめ測った複数の結果をJavaScriptで出し分けているだけ)。リアルタイムではなく、ローカルで測って焼き込み、更新時は測り直して再発行する方式。

**同じ方式で実測したところ、重要な発見があった:**

| ファイル | 切り出し前 | 切り出し後 |
|---|---|---|
| CLAUDE.md | 2,173字 | 2,180字 |
| **knowledge.md** | **4,673字** | **1,248字** |
| status.md | 1,001字 | 1,001字 |
| secretary.md | 842字 | 842字 |
| MEMORY.md(目次) | 676字 | 676字 |
| **起動時合計** | **9,365字** | **5,947字** |

ずっと「CLAUDE.mdが膨らむと薄まる」ことを気にしていたが、**実際に一番大きかったのはCLAUDE.md(2,173字)ではなくknowledge.md(4,673字)** で、2倍以上あった。しかもリサーチのたびに増える性質のファイルで、毎回全文読まれる。

**対応:** 部署ファイルと同じ切り出しルールをknowledge.mdにも適用し、2ファイルに分離した(起動時の読み込みが36%減)。
- `knowledge_knowhow.md` — 運用ノウハウ(ルールの「なぜ」と元になった失敗の記録)
- `knowledge_research.md` — リサーチ・トレンドメモ
- 本体には用語リストとリンクのみを残した。CLAUDE.md内の参照先2箇所も更新済み

**MD台帳の新設(CEO提案の切り分け):** CEOより「MD台帳はルール台帳の"どこに何があるか"を詳しくする機能。ルール台帳は組織構造に留め、MD台帳は切り出したほうがよい。文字数管理でベストパフォーマンスを維持するのは大事」との提案があり、独立したArtifactとして作成した。
- **ルール台帳** = 組織の構造とルール(何がどう決まっているか)
- **MD台帳** = ファイルの実態(何字あり、いつ読まれ、どこまで増えたら切り出すか)
- MD台帳には点検の目安も記載(起動時合計1万字以内/CLAUDE.md 2,500字以内/knowledge.md 1,500字以内/部署ファイル6,000字前後)
- 更新は「MD台帳を測り直して」と言えば、その場で実測して再発行する

**MD台帳の測り直しは都度依頼制で確定(2026-08-22・CEO判断)。** 定期実行をルール化するとCLAUDE.mdが1行増え、管理したい対象そのものを太らせるため。CEOが「測り直して」と言った時に秘書が実測して再発行する。

### 残った検討事項

- 現時点でなし

---

## 12. 進行中の案件(顧問部・海外畜産統計)

**この案件は例外的に、今回のセッションで秘書が最後まで対応することにCEOが合意している**(本来は顧問部に委譲すべきだが、経緯の引き継ぎコストを考慮した判断)。

詳細・進捗は [advisory_usda_project.md](advisory_usda_project.md) を参照。
