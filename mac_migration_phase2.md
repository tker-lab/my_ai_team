# Mac移行 第2段階手順書

## 前提

`mac_migration.md` の手順1〜11がすべて完了していることが前提です。**未完了の場合は、まずそちらを最後まで終わらせてから、この手順書に進んでください。** この手順書は「AIチームの環境構築が終わった後」の作業(LocalSendの導入、動画素材の移送、ライフサポート部ファイルの新規作成)を扱います。

この手順書を読んでいるMac側のClaude(あなた)へ: 今日この手順書がどういう経緯で作られたかの詳細は知らなくて大丈夫です。以下の指示だけに従って進めてください。CEOはIT初心者なので、専門用語には一言説明を添えてください。Macのユーザー名は `takahashitakayuki` です。

---

## 1. LocalSendのMac側導入

LocalSend(ローカルセンド)は、同じWi-Fiに繋がった端末同士でファイルを直接送り合えるアプリです。クラウドを経由しないので、大きい動画ファイルでも速く送れます。Windows側には既に導入済みです。

1. ブラウザで `https://localsend.org` を開く
2. Mac版をダウンロードしてインストール(CEOの操作が必要な場合はここで待って、完了を確認してから次へ)
3. インストール後にアプリを起動する

使い方: Mac・Windowsの両方でLocalSendを起動し、同じWi-Fiに繋がっていれば、相手の端末が自動で一覧に出てきます。CEOがWindows側からファイルを選んで送信すると、Mac側に受信通知が来ます。

## 2. 動画素材の移送(受け取りと配置)

CEOがLocalSendで以下のファイル・フォルダを送ります。**合計約1.2GB**あるので、Wi-Fiの状況によっては転送に数分〜十数分かかることがあります(ETA目安)。

送られてくるもの:
- `IMG_3156.MOV`(318MB)
- `IMG_3163.MOV`(640MB)
- `kotaro_MASTER_v5.mp4`(211MB)
- `チャンネル素材` フォルダ(約50MB、バナー・アイコン・背景のpng・htmlファイル)
- `サムネ` フォルダ(17MB)
- `切り抜き` フォルダ(11MB)

### 配置先

`~/Desktop/動画` フォルダの下に整理して配置してください(フォルダが無ければ作成)。「作業データはDesktop、AIチームの設定は `~/my_ai_team`」という切り分けに揃えるためです。

推奨する配置構成:

```
~/Desktop/動画/
  ├── 撮影素材/
  │     ├── IMG_3156.MOV
  │     └── IMG_3163.MOV
  ├── 編集済み/
  │     └── kotaro_MASTER_v5.mp4
  ├── チャンネル素材/
  └── サムネ/
  └── 切り抜き/
```

### 補足メモ(注記として残す)

- `IMG_3156.MOV` と `IMG_3163.MOV` は撮影素材(編集前の元動画)です。リポジトリ内の `scratch_3156.srt` / `scratch_3163.srt`(SRTは字幕ファイルの拡張子)は、それぞれこの2本の動画に対応する字幕データです。動画部の作業でこの2本を扱う際は対応関係を意識してください。
- `kotaro_MASTER_v5.mp4` は**現時点で最新の編集版**です。これより古いバージョンが別途出てきても、最新はこちらである旨を確認してから使ってください。

受け取り・配置が終わったら、`ls -la ~/Desktop/動画` 等でファイルサイズが送信元と一致していることを確認し、CEOに完了を報告してください(壊れていないかの簡易チェックです)。

## 3. ライフサポート部ファイルの新規作成(急ぎではない)

ライフサポート部の設定ファイル `life_team.md` はCEO家族の機微な情報を扱うため、`.gitignore`(Gitでの管理対象から除外するファイル)で除外されています。そのためGit経由の同期対象になっておらず、**Windows側の内容はMacに来ていません。これは意図した動作です。**

方針: **Windows版の内容を引き継ぐのではなく、Mac側でCEOに改めてヒアリングして新規に作り直します。** 上記1・2の作業が完了し、環境が一段落したタイミングで、CEOに次のように声をかけてください。

> 「ライフサポート部の設定ファイルは機密のため引き継いでいません。落ち着いたタイミングで、ご家族のサポートに関する内容を改めてヒアリングさせてください」

ヒアリングを踏まえて `~/my_ai_team/life_team.md` を新規作成してください。作成しても `.gitignore` により自動的にGit管理対象外になります(それが意図した動作です)。**急ぎの作業ではないので、他の作業を優先してよい**です。

## 4. 顧問部の移行について

顧問部(advisory)の移行手順は別ファイル `advisory_usda_mac_migration.md` に顧問部セッションが個別に作成しています。**この手順書ではその内容には立ち入りません。顧問部の移行はそちらを参照してください。**

## 5. 完了後の運用

上記1〜3(3は着手のみでも可)が終わったら、以下をCEOに報告してください。

- Windows機は以降「書庫」に格下げし、過去の動画アーカイブ(14.6GB)の置き場として残す
- 日常のAIチーム作業は今後Mac1台のみで行う

これでMacへの移行作業は一区切りです。
