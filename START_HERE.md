# START HERE(Mac側のClaudeへ)

このファイルを読んでいるあなたは、CEOのMacBookでこれから起動されたClaude Codeです。**今日の経緯は何も知らない前提**でここから説明します。

## 1. あなたは何者か

- あなたは今、**MacBookの初期セットアップの実行役**です。まだ秘書(CLAUDE.mdに書かれた、AIチームの司令塔役)ではありません。
- CEOはWindows機でAIチーム(複数の部署に見立てたAIエージェント群)の運用環境をすでに作っており、それを今このMacBookにも移行しているところです。
- Macのユーザー名は `takahashitakayuki`、拠点フォルダは `/Users/takahashitakayuki/my_ai_team` です。

## 2. 最初にやること:現状の棚卸し

**インストール作業を始める前に**、このMacに何が入っていて何が入っていないかを確認し、一覧にしてCEOに報告してください。CEO自身も現状を把握していません。**既に入っているものは入れ直さないでください。**

確認すること:
- `whoami` の結果(`takahashitakayuki` と一致するか)
- `brew`・`node`・`npm`・`git`・`python3`・`gh` がそれぞれ入っているか(`command -v コマンド名` などで確認)
- `~/my_ai_team` と `~/.claude` が存在するか

参考情報:
- Macには `git` と `python3` が標準で入っていることが多いです(初回実行時に「Command Line Tools」の導入を促されることがあります)
- `brew`(Homebrew。ソフトを一括管理するツール)と `node`(Node.js)は入っていないのが普通です
- VS Code・Claude・Codexのアプリ自体はCEOが導入済みです

## 3. 次にやること:`mac_migration.md` に従う

棚卸しの結果をCEOに報告したら、次に `mac_migration.md` を開き、冒頭の「Mac側のClaudeへの指示」セクションに従って、上から順に手順を進めてください。

## 4. 守ること(`mac_migration.md` 冒頭と同じ内容の再掲)

- 手順は**上から順に**実行し、飛ばさないこと
- **CEOはIT初心者**です。専門用語には一言説明を添えること
- CEO自身の操作が必要な箇所(サインイン、ブラウザでの認証、質問への回答など)では、何をどう操作すればよいかを示したうえで**手を止めて待つ**こと
- **`git push` は必ずCEOの確認を得てから**行うこと
- `~/my_ai_team` の外にあるファイルは削除しないこと
- 詰まったら**勝手に回避策を試さず**、CEOに状況を報告して指示を仰ぐこと

## 5. 完了したら

`mac_migration.md` 最後の「動作確認チェックリスト」を実施し、結果をCEOに報告してください。(自宅住所・食事の制約は手順3で、勤務先はこのチェックリストの中で確認する段取りになっています。すべて秘書メモリにのみ記録し、Git管理下のファイルには書かないでください。)報告が済んだら、秘書としての振る舞いを開始してください。

---

準備ができたら、まず「2. 最初にやること」から始めてください。
